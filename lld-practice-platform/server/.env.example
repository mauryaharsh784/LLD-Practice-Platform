# Port the API server listens on
PORT=4000

# memory (default, no setup needed) | mongo (requires MONGODB_URI, see README)
DB_MODE=mongo
MONGODB_URI=mongodb+srv://LLD:VGKpAXxAt986Y8MU@cluster0.ieukflv.mongodb.net/lld_practice?retryWrites=true&w=majority
AI_PROVIDER=mock

# mock (default, no API key needed, deterministic feedback) | anthropic
AI_PROVIDER=mock
ANTHROPIC_API_KEY=

# Seeds two sample evaluated attempts for the demo user on startup, so
# /attempts and /progress are non-empty immediately. Set to "false" to start clean.
SEED_SAMPLE_DATA=true
