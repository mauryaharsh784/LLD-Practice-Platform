# The client talks to the API through Vite's dev proxy (see vite.config.ts),
# so no environment variables are required to run the client in development.
# If you deploy the client separately from the API, set:
# VITE_API_BASE_URL=https://your-api-host/api
